import CreateFamily from "../createFamily";

function AddChild() {
  const lineStyle = { border: "6px solid green", height: "500px" };

  return <CreateFamily class="child" />;
}

export default AddChild;
