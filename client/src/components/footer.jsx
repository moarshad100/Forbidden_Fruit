export default function Footer() {
  const linkStyle = {
    border: "1px black",
    padding: "5px",
    display: "flex",
    gap: "10px",
    justifyContent: "center",
    alignItems: "center",
  };

  return (
    <section style={linkStyle}>
      <p>Footer</p>
    </section>
  );
}
